import { Experimental_LanguageModelV1Middleware } from "ai";

/**
 * Custom middleware for the language model.
 * This middleware can be used to modify or extend the behavior of the language model.
 */
export const customMiddleware: Experimental_LanguageModelV1Middleware = {};
